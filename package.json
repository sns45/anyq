{
  "name": "anyq-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/testers/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '@anyq/core' --filter '@anyq/memory' test",
    "lint": "echo 'Linter not yet configured'",
    "clean": "bun run --filter '*' clean",
    "dev": "bun run --filter '*' dev",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
